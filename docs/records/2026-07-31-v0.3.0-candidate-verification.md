# Loop Craft 0.3.0 候选确定性验证

> 日期：2026-07-31；候选代码：`704f9b4f2f25fdbcc527074e06b7f9404216e67a`；
> 环境：Windows，Python 3.13.13；
> 结论：确定性出口通过；产品阶段仍为 DRIFT

## 本次验证回答的风险

本次只回答共享合同修改后是否破坏 Schema、Compiler、Adapter、Evidence、verify 和三份保留
Demo。它不回答未知 Agent 是否能仅凭 Skill 顺畅完成用户路径，因此不能单独触发封板 PASS。

没有重建 Demo，没有执行真实用户项目，也没有重复全量盲测 population。

## 命令与结果

| 检查 | 命令范围 | 结果 |
|---|---|---|
| 全量确定性测试 | `python -m pytest -q` | `171 passed in 7.87s` |
| Python 编译 | `python -m compileall -q loop-craft/scripts tests` | passed |
| Schema meta-validation | Draft 2020-12 validator 检查两个 kernel schema | accepted-definition / entry-evidence 均通过 |
| Dashboard JSON | UTF-8 读取并解析 `dashboard/status.json` | passed |
| Skill 本地引用 | 解析 `loop-craft/**/*.md` 的本地 Markdown links | 0 broken |
| 官方 Skill validator | 官方 `skill-creator/scripts/quick_validate.py loop-craft` | `Skill is valid!` |

## 三份保留 Demo 的只读兼容性

当前 0.3.0 Core 对既有 build root 只执行 `verify`，不读取原始访谈/会话、不重建、不写回。

| Demo | 状态 | expected / actual artifact digest |
|---|---|---|
| From-scratch · project-asset-inventory | clean | `sha256:985c63266fa77dc7aa151c1332e6391239b9dbf8ea38c4937a9ae0a4e0830cf3` |
| Existing Skill · skill-polisher | clean | `sha256:a235cbb8fe4a6fb5cd51d73a34a9bd9bcac7ffd4ccb19b0e25c522ec207ced6b` |
| Conversation · project-handoff-dashboard-sync | clean | `sha256:ce1273c9f5ce661a25239a92d7f260159b86424b03004d4c897f6dc8a9d41e34` |

三者的 expected 与 actual 完全相等。旧 v0.1 Entry Evidence 与历史无 Entry Evidence build
继续可验证，说明 v0.2 Direct Build 合同没有破坏既有产物。

## 阶段判断

- 确定性候选：通过；
- Apache-2.0 / 0.3.0 事实：已落盘；
- 本机安装副本：尚未同步；
- unknown Agent 产品接口审计：尚未执行；
- 新候选远端 CI：尚未执行。

因此看板推进到“候选已验证”，但阶段出口继续显示 DRIFT。
